# Repository AI Instructions

Canonical source: `AGENTS.md`.

`CLAUDE.md` must remain a symlink to `AGENTS.md` for Claude ecosystem compatibility.

Repository skills live under `.claude/skills/`. They may add workflow-specific steps, but must not conflict with `AGENTS.md`.

When instructions conflict, follow `AGENTS.md`.
